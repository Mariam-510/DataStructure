Developing in C++ a class, BigInt that supports writing statements with extremely long integer values
Design the class BigInt:
BigInt (string decStr);
BigInt (int decInt);
BigInt operator+ (BigInt anotherDec);
BigInt operator= (BigInt anotherDec);
Int size();